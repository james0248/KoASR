# For local debugging
# conda env create -f koasr.yml
name: koasr
channels:
    - conda-forge
dependencies:
    - python=3.8
    - pytorch
    - numpy
    - pandas
    - transformers
    - datasets
    - pysoundfile
    - scikit-learn
    - librosa
    - pip
    - pip:
        - hangul_utils
        - git+https://github.com/n-CLAIR/nsml-local
